<Window
    x:Class="ProSchedules.UI.DuplicateSheetsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProSchedules"
    xmlns:ui="clr-namespace:ProSchedules.UI"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Pro Schedules"
    Width="600"
    Height="600"
    MinWidth="800"
    MinHeight="500"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    MouseMove="Window_MouseMove"
    PreviewKeyDown="Window_PreviewKeyDown"
    PreviewKeyUp="Window_PreviewKeyUp"
    mc:Ignorable="av">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FF1F1F1F" />
                    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#FF292929" />
                    <SolidColorBrush x:Key="BorderBrush" Color="#FF444444" />
                    <SolidColorBrush x:Key="ForegroundBrush" Color="#EEEEEE" />
                </ResourceDictionary>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/ProSchedules;component/UI/Themes/ElementStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <ui:InverseBooleanConverter x:Key="InverseBooleanConverter"/>





            
            <Style x:Key="CustomCancelButtonStyle" TargetType="{x:Type Button}" BasedOn="{StaticResource CustomActionButtonStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                                <ContentPresenter
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Content}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <!--  Hover Effect  -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#FF6B6B" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#FF6B6B" />
                                </Trigger>
                                <!--  Pressed Effect  -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="ButtonBorder" Property="Background" Value="#D32F2F" />
                                    <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>


    <Grid Background="Transparent" ClipToBounds="True">
        <!-- Outer Border with Opacity and Blur -->
        <Border
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="2"
            ClipToBounds="True"
            CornerRadius="10"
            Opacity="0.7"
            SnapsToDevicePixels="True">
            <Border.Effect>
                <BlurEffect KernelType="Gaussian" Radius="2" />
            </Border.Effect>
        </Border>

        <!-- Content Container -->
        <Border
            Margin="2"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            CornerRadius="10">
            <Grid x:Name="MainContentGrid" PreviewMouseDown="Window_PreviewMouseDown">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                    <!-- Row 0: Title bar -->
                    <RowDefinition Height="*" />
                    <!-- Row 1: Content -->
                </Grid.RowDefinitions>

                <!-- Title bar (Row 0) -->
                <ui:TitleBar
                    Grid.Row="0"
                    Foreground="{DynamicResource ForegroundBrush}"
                    Loaded="TitleBar_Loaded" />
        <!-- Sorting Popup Overlay Removed (Moved to SortingWindow) -->
        
                <!-- Theme toggle -->
                <ToggleButton
                    x:Name="ThemeToggleButton"
                    Grid.Row="0"
                    Width="28"
                    Height="15.5"
                    Margin="0,0,70,0"
                    HorizontalAlignment="Right"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="ToggleTheme_Click"
                    IsChecked="True"
                    ToolTip="Toggle Theme">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                            <Grid Background="Transparent">
                                <materialDesign:PackIcon
                                    x:Name="ThemeToggleIcon"
                                    Width="30"
                                    Height="30"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource IconBrush}"
                                    Kind="ToggleSwitchOffOutline" />
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOffOutline" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="False">
                                    <Setter TargetName="ThemeToggleIcon" Property="Kind" Value="ToggleSwitchOutline" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>

                <!-- Main Content (Row 1) -->
                <Grid Grid.Row="1" Margin="10,10,10,10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Top Bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">

                            <Button x:Name="RenameButton"
                                    Click="Rename_Click"
                                    Style="{StaticResource CustomActionButtonStyle}"
                                    ToolTip="Batch Rename"
                                    Width="32" Height="32"
                                    Margin="0,0,6,0">
                                <materialDesign:PackIcon Kind="FindReplace" Width="20" Height="20"/>
                            </Button>

                            <Button Click="Parameters_Click"
                                    Style="{StaticResource CustomActionButtonStyle}"
                                    Width="32" Height="32"
                                    ToolTip="Parameters"
                                    Margin="0,0,6,0">
                                <materialDesign:PackIcon Kind="TuneVariant" Width="20" Height="20"/>
                            </Button>

                            <Button x:Name="SortButton"
                                    Click="Sort_Click"
                                    Style="{StaticResource CustomActionButtonStyle}"
                                    ToolTip="Sort Settings"
                                    Width="32" Height="32"
                                    Margin="0,0,6,0">
                                <materialDesign:PackIcon Kind="Sort" Width="20" Height="20"/>
                            </Button>

                            <Button Click="HighlightInModel_Click"
                                    Style="{StaticResource CustomActionButtonStyle}"
                                    ToolTip="Highlight in Model"
                                    Width="32" Height="32"
                                    Margin="0,0,6,0">
                                <materialDesign:PackIcon Kind="FileFindOutline" Width="20" Height="20"/>
                            </Button>
                        </StackPanel>

                        <!-- Schedule Select and Itemize -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left" Margin="10,0,0,0">
                            <ComboBox x:Name="SchedulesComboBox"
                                      Width="200"
                                      Height="32"
                                      Margin="0,0,10,0"
                                      SelectionChanged="SchedulesComboBox_SelectionChanged"
                                      DisplayMemberPath="Name"
                                      Style="{StaticResource DarkComboBoxStyle}"/>
                            
                            <CheckBox x:Name="ItemizeCheckBox"
                                      Content="Itemize every instance"
                                      VerticalAlignment="Center"
                                      Foreground="{DynamicResource ForegroundBrush}"
                                      Checked="Itemize_Checked"
                                      Unchecked="Itemize_Unchecked"
                                      IsChecked="True"
                                      Margin="0,0,10,0"
                                      Style="{StaticResource DarkCheckBoxStyle}"/>
                        </StackPanel>

                        <!-- Search Box with Magnify Icon -->
                        <Border Grid.Column="1"
                                Width="140"
                                Height="24"
                                HorizontalAlignment="Right"
                                Background="{DynamicResource SecondaryBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Opacity="0.95">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="28"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="28"/>
                                </Grid.ColumnDefinitions>

                                <materialDesign:PackIcon Kind="Magnify"
                                                       Grid.Column="0"
                                                       Width="16" Height="16"
                                                       HorizontalAlignment="Center"
                                                       VerticalAlignment="Center"
                                                       Foreground="{DynamicResource IconBrush}"/>

                                <TextBlock Grid.Column="1"
                                          Text="Search"
                                          Foreground="#FF808080"
                                          VerticalAlignment="Center"
                                          Margin="10,0,0,0"
                                          IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SheetSearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBox Grid.Column="1"
                                         x:Name="SheetSearchBox"
                                         Style="{StaticResource CustomTextBoxStyle}"
                                         Height="Auto"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Stretch"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         TextChanged="SheetSearch_TextChanged"/>

                                <Button Grid.Column="2"
                                        Width="24"
                                        Height="24"
                                        Click="ClearSheetSearch_Click"
                                        ToolTip="Clear search"
                                        Style="{StaticResource FlatIconButton}">
                                    <materialDesign:PackIcon Kind="CloseCircleOutline" 
                                                            Width="14" 
                                                            Height="14"
                                                            Foreground="{DynamicResource IconBrush}"/>
                                </Button>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Sheets List -->
                    <!-- Removed title by setting Header to null/empty and ensuring style handles it, or just empty string -->
                    <GroupBox Grid.Row="1" Grid.Column="0" Header="{x:Null}" 
                              Margin="0"
                              Style="{StaticResource FullWidthGroupBoxStyle}">
                        <Grid>
                            <DataGrid
                                      x:Name="SheetsDataGrid"
                                      ItemsSource="{Binding FilteredSheets}" 
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False" 
                                      CanUserDeleteRows="False" 
                                      CanUserResizeRows="False"
                                      Style="{StaticResource CustomDataGridStyle}"
                                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                                      CellStyle="{StaticResource ExcelLikeCellStyle}"
                                      SelectionMode="Extended"
                                      SelectionUnit="Cell"
                                      AlternationCount="2"
                                      RowStyle="{StaticResource ExcelLikeRowStyle}"
                                      RowHeight="20"
                                      GridLinesVisibility="None"
                                      HeadersVisibility="Column"
                                      RowHeaderWidth="0"
                                      Background="{DynamicResource PrimaryBackgroundBrush}"
                                      Margin="0,1,0,0"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Auto"
                                      SelectionChanged="SheetsDataGrid_SelectionChanged"
                                      CellEditEnding="SheetsDataGrid_CellEditEnding"
                                      PreviewMouseWheel="SheetsDataGrid_PreviewMouseWheel"
                                      KeyDown="SheetsDataGrid_KeyDown">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Width="35" HeaderStyle="{StaticResource MultiRowHeaderStyle}">
                                        <DataGridTemplateColumn.Header>
                                            <CheckBox Style="{StaticResource DatagridCheckboxStyle}"
                                                      HorizontalAlignment="Center"
                                                      Checked="SelectAllSheets_Checked"
                                                      Unchecked="SelectAllSheets_Unchecked"
                                                      ToolTip="Select/Deselect All Visible"/>
                                        </DataGridTemplateColumn.Header>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                             Style="{StaticResource DatagridCheckboxStyle}"
                                                             Focusable="False"
                                                             PreviewMouseLeftButtonDown="SheetCheckBox_PreviewMouseLeftButtonDown"/>
                                                </Grid>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTextColumn Header="Nr"
                                                        Binding="{Binding SheetNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                        IsReadOnly="False"
                                                        Width="SizeToCells"
                                                        HeaderStyle="{StaticResource MultiRowHeaderStyle}"
                                                        ElementStyle="{StaticResource EditableCellStyle}"
                                                        CellStyle="{StaticResource ExcelLikeCellStyle}"/>
                                    <DataGridTextColumn Header="Name"
                                                        Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                        IsReadOnly="False"
                                                        Width="*"
                                                        HeaderStyle="{StaticResource MultiRowHeaderStyle_NoRightBorder}"
                                                        ElementStyle="{StaticResource EditableCellStyle}"
                                                        CellStyle="{StaticResource ExcelLikeCellStyle}"/>
                                </DataGrid.Columns>
                            </DataGrid>
                            
                            <!-- Selection Overlay Canvas -->
                            <Canvas x:Name="SelectionCanvas" ClipToBounds="True">
                                <!-- Selection Outline (Green Border) -->
                                <Border x:Name="SelectionBox"
                                        BorderBrush="#70babc"
                                        BorderThickness="2"
                                        IsHitTestVisible="False"
                                        Visibility="Collapsed"
                                        SnapsToDevicePixels="True"/>
                                
                                <!-- Fill Handle (Bottom Right Grip) -->
                                <Thumb x:Name="FillHandle"
                                       Width="6" Height="6"
                                       Background="#70babc"
                                       Cursor="Cross"
                                       Visibility="Collapsed"
                                       IsHitTestVisible="True"
                                       DragDelta="FillHandle_DragDelta"
                                       DragCompleted="FillHandle_DragCompleted">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="{x:Type Thumb}">
                                            <Border Background="{TemplateBinding Background}"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>

                                <!-- Fill Indicator (+) -->
                                <Border x:Name="FillIndicator"
                                        Visibility="Collapsed"
                                        IsHitTestVisible="False">
                                    <TextBlock Text="+" 
                                               Foreground="{DynamicResource ForegroundBrush}" 
                                               FontSize="20" 
                                               FontWeight="Bold"/>
                                </Border>
                            </Canvas>
                        </Grid>
                    </GroupBox>
                </Grid>

            </Grid>
        </Border>

                <!-- Duplicate Options Popup Overlay -->
                <Grid x:Name="OptionsPopupOverlay" Grid.RowSpan="3" Visibility="Collapsed" Panel.ZIndex="1050">
                     <!-- Dim Background -->
                     <Border Background="#90000000" MouseLeftButtonDown="OptionsPopupBackground_Click"/>

                     <!-- Options Content -->
                     <Border HorizontalAlignment="Center" 
                             VerticalAlignment="Center"
                             Width="400"
                             Padding="20"
                             Background="{DynamicResource PrimaryBackgroundBrush}"
                             BorderBrush="{DynamicResource BorderBrush}"
                             BorderThickness="1"
                             CornerRadius="8">
                         <Border.Effect>
                             <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.4"/>
                         </Border.Effect>

                         <StackPanel>
                             <TextBlock Text="Duplicate Options" 
                                        FontSize="18" 
                                        FontWeight="Bold" 
                                        Foreground="{DynamicResource ForegroundBrush}"
                                        Margin="0,0,0,15"
                                        HorizontalAlignment="Center"/>

                             <!-- Form Layout Grid -->
                             <Grid Margin="0,0,0,15">
                                 <Grid.RowDefinitions>
                                     <RowDefinition Height="Auto"/>
                                     <RowDefinition Height="Auto"/>
                                 </Grid.RowDefinitions>
                                 <Grid.ColumnDefinitions>
                                     <ColumnDefinition Width="Auto"/>
                                     <ColumnDefinition Width="*"/>
                                 </Grid.ColumnDefinitions>

                                 <!-- Row 0: Copies -->
                                 <TextBlock Grid.Row="0" Grid.Column="0" 
                                            Text="Copies:" 
                                            VerticalAlignment="Center" 
                                            Foreground="{DynamicResource ForegroundBrush}"
                                            Margin="0,0,15,0"/>
                                 <TextBox x:Name="OptCopies" Grid.Row="0" Grid.Column="1" 
                                          Width="60" 
                                          Text="1" 
                                          Style="{StaticResource CustomTextBoxStyle}"
                                          HorizontalAlignment="Right"
                                          HorizontalContentAlignment="Center"/>

                                 <!-- Row 1: Duplicate Type replaced by checkbox logic below -->
                             </Grid>

                             <!-- Checkboxes -->
                             <CheckBox x:Name="OptKeepViews" Content="Keep Views" IsChecked="False" Margin="0,5" Foreground="{DynamicResource ForegroundBrush}" Style="{StaticResource CustomCheckboxStyle}"/>
                             <CheckBox x:Name="OptKeepLegends" Content="Keep Legends" IsChecked="False" Margin="0,5" Foreground="{DynamicResource ForegroundBrush}" Style="{StaticResource CustomCheckboxStyle}"/>
                             <CheckBox x:Name="OptKeepSchedules" Content="Keep Schedules" IsChecked="False" Margin="0,5" Foreground="{DynamicResource ForegroundBrush}" Style="{StaticResource CustomCheckboxStyle}"/>
                             <CheckBox x:Name="OptCopyRevisions" Content="Copy Revisions" IsChecked="True" Margin="0,5" Foreground="{DynamicResource ForegroundBrush}" Style="{StaticResource CustomCheckboxStyle}"/>
                             <CheckBox x:Name="OptCopyParams" Content="Copy Title Block Parameter Values" IsChecked="True" Margin="0,5" Foreground="{DynamicResource ForegroundBrush}" Style="{StaticResource CustomCheckboxStyle}"/>

                             <!-- Action Buttons -->
                             <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                                 <Button Content="Duplicate" 
                                         Click="DuplicateConfirm_Click"
                                         Style="{StaticResource CustomActionButtonStyle}"
                                         Width="120" Height="35"
                                         FontSize="12"
                                         Margin="5,0,5,0"/>
                                 
                                 <Button Content="Cancel" 
                                         Click="DuplicateCancel_Click"
                                         Style="{StaticResource NegativeActionButtonStyle}"
                                         Background="#FF555555"
                                         BorderBrush="#FF555555"
                                         Width="120" Height="35"
                                         FontSize="12"
                                         Margin="5,0,5,0"/>
                             </StackPanel>
                         </StackPanel>
                     </Border>
                </Grid>

                <!-- Generic Popup Overlay (Keep for errors etc) -->
                <Grid x:Name="PopupOverlay" Grid.RowSpan="3" Visibility="Collapsed" Panel.ZIndex="1000">
                    <!-- Dim Background -->
                    <Border Background="#80000000" MouseLeftButtonDown="PopupBackground_Click"/>

                    <!-- Popup Content -->
                    <Border HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Width="300"
                            Padding="20"
                            Background="{DynamicResource PrimaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.4"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="PopupTitle" 
                                       Grid.Row="0"
                                       Text="Title" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,10"/>

                            <TextBlock x:Name="PopupMessage" 
                                       Grid.Row="1"
                                       Text="Message content goes here." 
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,20"/>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="PopupCloseButton"
                                        Content="OK"
                                        Click="PopupClose_Click"
                                        Style="{StaticResource CustomActionButtonStyle}"
                                        Width="80" 
                                        Height="30"
                                        FontSize="12"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Confirm Popup Overlay -->
                <Grid x:Name="ConfirmPopupOverlay" Grid.RowSpan="3" Visibility="Collapsed" Panel.ZIndex="1100">
                    <!-- Dim Background -->
                    <Border Background="#80000000" MouseLeftButtonDown="ConfirmPopupBackground_Click"/>

                    <!-- Popup Content -->
                    <Border HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Width="320"
                            Padding="20"
                            Background="{DynamicResource PrimaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.4"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="ConfirmPopupTitle" 
                                       Grid.Row="0"
                                       Text="Title" 
                                       FontSize="16" 
                                       FontWeight="Bold" 
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,10"/>

                            <TextBlock x:Name="ConfirmPopupMessage" 
                                       Grid.Row="1"
                                       Text="Message content goes here." 
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,20"/>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button x:Name="ConfirmActionButton"
                                        Content="Discard"
                                        Click="ConfirmDiscard_Click"
                                        Style="{StaticResource CustomActionButtonStyle}"
                                        Width="90" 
                                        Height="30"
                                        FontSize="12"
                                        Margin="0,0,8,0"/>
                                <Button Content="Cancel"
                                        Click="ConfirmCancel_Click"
                                        Style="{StaticResource NegativeActionButtonStyle}"
                                        Background="#FF555555"
                                        BorderBrush="#FF555555"
                                        Width="90" 
                                        Height="30"
                                        FontSize="12"
                                        Margin="0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Parameters Popup Overlay -->
                <Grid x:Name="ParametersPopupOverlay" Grid.RowSpan="3" Visibility="Collapsed" Panel.ZIndex="1050">
                    <!-- Dim Background -->
                    <Border Background="#80000000" MouseLeftButtonDown="ParametersPopupBackground_Click"/>

                    <!-- Popup Content -->
                    <Border HorizontalAlignment="Center" 
                            VerticalAlignment="Center"
                            Width="360"
                            Padding="20"
                            Background="{DynamicResource PrimaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.4"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0"
                                       Text="Parameters"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,10"
                                       HorizontalAlignment="Center"/>

                            <TextBlock Grid.Row="1"
                                       Text="Parameter tools coming soon."
                                       TextWrapping="Wrap"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,20"/>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Close"
                                        Click="ParametersClose_Click"
                                        Style="{StaticResource CustomActionButtonStyle}"
                                        Width="90" 
                                        Height="30"
                                        FontSize="12"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <!-- Rename Popup Overlay -->
                <Grid x:Name="RenamePopupOverlay" Grid.RowSpan="3" Visibility="Collapsed" Panel.ZIndex="1050">
                    <!-- Dim Background -->
                    <Border Background="#90000000" MouseLeftButtonDown="RenamePopupBackground_Click"/>

                    <!-- Rename Content -->
                    <Border HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Width="500"
                            MaxHeight="700"
                            Padding="20"
                            Background="{DynamicResource PrimaryBackgroundBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.4"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Title -->
                            <TextBlock Grid.Row="0"
                                       Text="Batch Rename parameter values"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource ForegroundBrush}"
                                       Margin="0,0,0,15"
                                       HorizontalAlignment="Center"/>

                            <!-- Rename Options -->
                            <Grid Grid.Row="1" Margin="0,0,0,15">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Parameter -->
                                <TextBlock Grid.Row="0" Grid.Column="0"
                                           Text="Parameter:"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,0,15,10"/>
                                <ComboBox x:Name="RenameParameter"
                                          Grid.Row="0" Grid.Column="1"
                                          Width="220"
                                          HorizontalAlignment="Left"
                                          SelectedIndex="0"
                                          Margin="0,0,0,10"
                                          SelectionChanged="RenameParameter_Changed"
                                          Style="{StaticResource DarkThemeComboBox}"
                                          ItemsSource="{Binding RenameParameterOptions}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Name}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>



                                <!-- Find -->
                                <TextBlock Grid.Row="1" Grid.Column="0"
                                           Text="Find:"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,0,15,10"/>
                                <TextBox x:Name="RenameFindText"
                                         Grid.Row="1" Grid.Column="1"
                                         Width="220"
                                         HorizontalAlignment="Left"
                                         Style="{StaticResource CustomTextBoxStyle}"
                                         Margin="0,0,0,10"
                                         TextChanged="RenameText_Changed"/>

                                <!-- Replace -->
                                <TextBlock Grid.Row="2" Grid.Column="0"
                                           Text="Replace:"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,0,15,10"/>
                                <TextBox x:Name="RenameReplaceText"
                                         Grid.Row="2" Grid.Column="1"
                                         Width="220"
                                         HorizontalAlignment="Left"
                                         Style="{StaticResource CustomTextBoxStyle}"
                                         Margin="0,0,0,10"
                                         TextChanged="RenameText_Changed"/>

                                <!-- Prefix -->
                                <TextBlock Grid.Row="3" Grid.Column="0"
                                           Text="Prefix:"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,0,15,10"/>
                                <TextBox x:Name="RenamePrefixText"
                                         Grid.Row="3" Grid.Column="1"
                                         Width="220"
                                         HorizontalAlignment="Left"
                                         Style="{StaticResource CustomTextBoxStyle}"
                                         Margin="0,0,0,10"
                                         TextChanged="RenameText_Changed"/>

                                <!-- Suffix -->
                                <TextBlock Grid.Row="4" Grid.Column="0"
                                           Text="Suffix:"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource ForegroundBrush}"
                                           Margin="0,0,15,0"/>
                                <TextBox x:Name="RenameSuffixText"
                                         Grid.Row="4" Grid.Column="1"
                                         Width="220"
                                         HorizontalAlignment="Left"
                                         Style="{StaticResource CustomTextBoxStyle}"
                                         TextChanged="RenameText_Changed"/>
                            </Grid>

                            <!-- Preview List -->
                            <Border Grid.Row="2"
                                    Background="{DynamicResource SecondaryBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Margin="0,0,0,15">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="Preview"
                                               FontSize="14"
                                               FontWeight="Bold"
                                               Foreground="{DynamicResource ForegroundBrush}"
                                               Margin="10,10,10,5"/>

                                    <DataGrid Grid.Row="1"
                                              x:Name="RenamePreviewDataGrid"
                                              ItemsSource="{Binding RenamePreviewItems}"
                                              AutoGenerateColumns="False"
                                              IsReadOnly="True"
                                              CanUserAddRows="False"
                                              CanUserDeleteRows="False"
                                              CanUserResizeRows="False"
                                              Style="{StaticResource CustomDataGridStyle}"
                                              SelectionMode="Single"
                                              RowBackground="{DynamicResource PrimaryBackgroundBrush}"
                                              AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                                              RowHeight="24"
                                              GridLinesVisibility="None"
                                              HeadersVisibility="Column"
                                              RowHeaderWidth="0"
                                              Background="Transparent"
                                              Margin="5">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Original"
                                                                Binding="{Binding Original}"
                                                                Width="*"
                                                                HeaderStyle="{StaticResource MultiRowHeaderStyle}"/>
                                            <DataGridTextColumn Header="New"
                                                                Binding="{Binding New}"
                                                                Width="*"
                                                                HeaderStyle="{StaticResource MultiRowHeaderStyle_NoRightBorder}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Grid>
                            </Border>

                            <!-- Action Buttons -->
                              <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
                                  <Button Content="Apply"
                                          Click="RenameApply_Click"
                                          Style="{StaticResource CustomActionButtonStyle}"
                                          Width="120" Height="35"
                                          FontSize="12"
                                          Margin="5,0,5,0"/>

                                  <Button Content="Cancel"
                                          Click="RenameCancel_Click"
                                          Style="{StaticResource NegativeActionButtonStyle}"
                                          Background="#FF555555"
                                          BorderBrush="#FF555555"
                                          Width="120" Height="35"
                                          FontSize="12"
                                          Margin="5,0,5,0"/>
                              </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
        <!-- Confirmation Popup Overlay -->
        <Grid x:Name="ConfirmationPopupOverlay" Visibility="Collapsed" Background="#AA000000">
            <Border Background="{DynamicResource PrimaryBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="2"
                    CornerRadius="8"
                    Width="400"
                    Padding="20"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="ConfirmationTitle"
                               Text="Confirmation"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Margin="0,0,0,15"/>
                    <TextBlock x:Name="ConfirmationMessage"
                               Text="Are you sure?"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource ForegroundBrush}"
                               Margin="0,0,0,20"/>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">
                        <Button Content="OK"
                                Click="ConfirmationOK_Click"
                                Style="{StaticResource CustomActionButtonStyle}"
                                Width="100"
                                Height="35"
                                Margin="0,0,10,0"/>
                        <Button Content="Cancel"
                                Click="ConfirmationCancel_Click"
                                Style="{StaticResource CustomCancelButtonStyle}"
                                Width="100"
                                Height="35"
                                Margin="0,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Resize edges -->
        <Border x:Name="LeftEdge" Width="5" HorizontalAlignment="Left" VerticalAlignment="Stretch"
                Background="Transparent" Cursor="SizeWE" Panel.ZIndex="3000"
                MouseEnter="LeftEdge_MouseEnter" MouseLeave="Edge_MouseLeave"
                MouseLeftButtonDown="LeftEdge_MouseLeftButtonDown" />
        <Border x:Name="RightEdge" Width="9" HorizontalAlignment="Right" VerticalAlignment="Stretch"
                Background="Transparent" Cursor="SizeWE" Panel.ZIndex="3000"
                MouseEnter="RightEdge_MouseEnter" MouseLeave="Edge_MouseLeave"
                MouseLeftButtonDown="RightEdge_MouseLeftButtonDown" />
        <Border x:Name="BottomEdge" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Bottom"
                Background="Transparent" Cursor="SizeNS" Panel.ZIndex="3000"
                MouseEnter="BottomEdge_MouseEnter" MouseLeave="Edge_MouseLeave"
                MouseLeftButtonDown="BottomEdge_MouseLeftButtonDown" />
        <Border x:Name="BottomLeftCorner" Width="10" Height="10" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                Background="Transparent" Cursor="SizeNESW" Panel.ZIndex="3000"
                MouseEnter="BottomLeftCorner_MouseEnter" MouseLeave="Edge_MouseLeave"
                MouseLeftButtonDown="BottomLeftCorner_MouseLeftButtonDown" />
        <Border x:Name="BottomRightCorner" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Background="Transparent" Cursor="SizeNWSE" Panel.ZIndex="3000"
                MouseEnter="BottomRightCorner_MouseEnter" MouseLeave="Edge_MouseLeave"
                MouseLeftButtonDown="BottomRightCorner_MouseLeftButtonDown" />
    </Grid>
</Window>
