<Window x:Class="ProSchedules.UI.ParametersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:ui="clr-namespace:ProSchedules.UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" 
        Title="Schedule Parameters" Height="500" Width="700"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" />
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FF1F1F1F" />
                    <SolidColorBrush x:Key="SecondaryBackgroundBrush" Color="#FF292929" />
                    <SolidColorBrush x:Key="BorderBrush" Color="#FF444444" />
                    <SolidColorBrush x:Key="ForegroundBrush" Color="#EEEEEE" />
                </ResourceDictionary>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Dark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/ProSchedules;component/UI/Themes/ElementStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="ListBoxItemStyle" TargetType="ListBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd" 
                                    Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Padding="{TemplateBinding Padding}" 
                                    SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#66007ACC"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#1E1E1E" 
            BorderBrush="#333333" 
            BorderThickness="1" 
            CornerRadius="8">
        <Grid Margin="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- TitleBar -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <ui:TitleBar Grid.Row="0" Foreground="{DynamicResource ForegroundBrush}" IsMinimizeVisible="False" />

            <!-- Main Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Available Parameters -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Available Fields:" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5" FontWeight="Bold"/>
                    <Border Grid.Row="1" CornerRadius="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource SecondaryBackgroundBrush}" ClipToBounds="True">
                        <ListBox x:Name="AvailableParamsList" 
                                 DisplayMemberPath="Name" 
                                 SelectionMode="Extended"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ItemContainerStyle="{StaticResource ListBoxItemStyle}"/>
                    </Border>
                </Grid>

                <!-- Add/Remove Buttons -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                    <Button Content="&gt;" Click="Add_Click" Width="30" Margin="0,0,0,10" Style="{StaticResource CustomActionButtonStyle}" ToolTip="Add Selected" Padding="0"/>
                    <Button Content="&lt;" Click="Remove_Click" Width="30" Style="{StaticResource CustomActionButtonStyle}" ToolTip="Remove Selected" Padding="0"/>
                </StackPanel>

                <!-- Scheduled Fields -->
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Scheduled Fields (in order):" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,5" FontWeight="Bold"/>
                    <Border Grid.Row="1" CornerRadius="5" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Background="{DynamicResource SecondaryBackgroundBrush}" ClipToBounds="True">
                        <ListBox x:Name="ScheduledParamsList" 
                                 DisplayMemberPath="Name" 
                                 SelectionMode="Extended"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ItemContainerStyle="{StaticResource ListBoxItemStyle}"
                                 AllowDrop="True"
                                 PreviewMouseLeftButtonDown="ScheduledParamsList_PreviewMouseLeftButtonDown"
                                 PreviewMouseMove="ScheduledParamsList_PreviewMouseMove"
                                 Drop="ScheduledParamsList_Drop"
                                 DragOver="ScheduledParamsList_DragOver"/>
                    </Border>
                </Grid>

                <!-- Sorting Buttons -->
                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="10,0">
                    <Button Content="&#x25B2;" Click="MoveUp_Click" Width="30" Margin="0,0,0,10" Style="{StaticResource CustomActionButtonStyle}" ToolTip="Move Up" Padding="0"/>
                    <Button Content="&#x25BC;" Click="MoveDown_Click" Width="30" Style="{StaticResource CustomActionButtonStyle}" ToolTip="Move Down" Padding="0"/>
                </StackPanel>
            </Grid>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,20">
                <Button Content="Apply" Click="Apply_Click" 
                        Style="{StaticResource CustomActionButtonStyle}" 
                        Width="100" Height="35" FontSize="12" Margin="0,0,10,0"/>
                <Button Content="Cancel" Click="Cancel_Click" 
                        Style="{StaticResource NegativeActionButtonStyle}" 
                        Background="#FF555555" BorderBrush="#FF555555" 
                        Width="100" Height="35" FontSize="12" Margin="0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
